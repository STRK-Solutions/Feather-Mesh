---
- name: Preserve explicit W1 private operator deployment
  ansible.builtin.include_tasks: w1.yml
  when: not (feam_participant_services | default(false) | bool)

- name: Install reviewed participant service release
  ansible.builtin.include_tasks: participant.yml
  when: feam_participant_services | default(false) | bool
