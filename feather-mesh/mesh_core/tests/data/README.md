# Feather Mesh Test Fixtures

This directory contains static, deterministic dummy data used for testing Feather Mesh filesystem interactions.

It is intended to simulate published data products on the shared HPC filesystem for local development and CI tests, ensuring tests do not require external filesystem setup.
